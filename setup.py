"""Setup configuration for bachtrackapi package."""
from setuptools import setup, find_packages

setup(
    name="bachtrackapi",
    version="0.1.2",
    packages=find_packages(),
    python_requires=">=3.8",
    include_package_data=True,
)
